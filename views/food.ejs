<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= category %></title>
</head>
<body>
    <%- include('./partials/nav') %>
    <h1><%= category %></h1>
    <% foundFood.forEach(item=> {%>
        <h3>Name: <%= item.name %>, Price: <%= item.price %>, rating: <%= item.rating %>, details: <%= item.details %></h3><br>
    <%}) %>
</body>
</html>